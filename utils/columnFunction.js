// Function to calculate area of Square column section
function calculateColumnSectionArea(columnWidth) {
    // Calculate area of the Square Column Section
    const columnSectionArea = columnWidth * columnWidth;
    return columnSectionArea;
}

module.exports = {
    calculateColumnSectionArea
};